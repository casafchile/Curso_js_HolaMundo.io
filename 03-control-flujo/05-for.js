/*for (inicialization; comparacion; incremento) {
    // Código a ejecutar en cada iteración
}
*/

for (let i=2; i <= 8; i++) { // Inicializa i en 0, verifica si i es menor o igual a 10, y lo incrementa en 1 en cada iteración
    if (i % 2 == 0) { // Verifica si i es par
        console.log(i + " es par"); // Imprime si i es par
    }
}